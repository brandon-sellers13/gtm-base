# Handoff prompt, 2026-09-05

Paste into a new chat opened in ~/Build/gtm-base.

Continue building GTM Base. Working directory: ~/Build/gtm-base (docs only today; not yet a git repository).

Read these first, in this order:
1. docs/plans/2026-09-04-001-feat-current-without-integrations-plan.md (revision 2, plus Amendments r2.1 to r2.3 at the end)
2. docs/plans/2026-09-05-001-feat-join-and-onboarding-plan.md (revision 2; its "Changes to the first feature's units" section modifies the first plan and must be honored while building it)
3. docs/brainstorms/2026-09-04-current-without-integrations-requirements.md and docs/brainstorms/2026-09-05-join-and-onboarding-requirements.md (the origins; the plans win where they differ and say why)
4. The parent scope: ~/Obsidian/Vault/Work/GTM-Base/2026-09-04-gtm-base-scope-v1.md
5. Doctrine: the "AI-native definition" block in ~/Personal/CLAUDE.md

Then run /compound-engineering:ce-work on the first plan, Phase A1, in this order: Unit 1 (scaffold, manifests, template), Unit 2 (library), Unit 9a (stale library), Unit 4 (gate), Unit 3 (session-start hook), Unit 7 (propose-change), Unit 9b (stale-check skill), Unit 10 (confirm). Apply the join plan's changes to each of those units as you build them rather than retrofitting later.

Execution posture (decided): one Opus 5 subagent at high reasoning effort per implementation unit. You orchestrate: hold both plans and the tests, brief each agent with its unit text plus the relevant "changes" lines, run the tests and the plain-language lint after each unit, and do not merge a unit whose test scenarios are not all covered. Python 3 standard library only; POSIX sh hook wrappers; python3 -m unittest under tests/; a fake gh on PATH under tests/fakes/.

Gates and rules:
- Before the first commit, ask me to confirm the GitHub remote and visibility; do not create or push a remote without my yes. Initializing the local repository is fine.
- SC1 and SC5 from the first plan must pass end to end on a real test base before Phase A2 starts.
- The proposal body is the single artifact; the v2 web app will render it unchanged.
- No MCP servers declared by the plugin; no network call at context-read time; nothing written outside the base, the seat directory (~/.gtm-base), and a configured global hooks directory.
- Never present an invented number or timing as a result. Walkthrough documents are written after real runs.

Keep memory current: ~/.claude/projects/-Users-brandonsellers-Personal/memory/gtm-base-product-direction.md holds the decision trail; append when a decision changes.

Start by confirming you have read all five items and listing the first unit's files and test scenarios, then begin.
