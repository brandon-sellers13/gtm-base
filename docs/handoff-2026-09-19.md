# Handoff, 2026-09-19

Paste into a new chat. Working directory: ~/Build/gtm-base (public repository brandon-sellers13/gtm-base, branch main, plugin 0.2.6 installed on this machine as gtm-base@gtm-base).

## Read first, in this order

1. docs/plans/2026-09-05-001-feat-join-and-onboarding-plan.md, Amendment r2.4 (sections A to I) at the end.
2. docs/plans/2026-09-04-001-feat-current-without-integrations-plan.md, Amendment r2.5 at the end.
3. docs/reviews/2026-09-19-codex-setup-shape-verdict.md (Codex's conditions; section "Smallest ordered change set" is the build order).
4. docs/diagrams/logic-atlas.html (every rule, one figure each; update it in the same change as the code and republish to artifact 847c38c1-6d13-4fdd-aa89-027ad1674983).
5. The memory file gtm-base-product-direction.md for the decision trail.

## State

- Phase A1 of the first feature and release one of the join plan are built and shipped (0.2.6, 1127 tests, `sh tests/run.sh`).
- One real base exists: ~/GTM Bases/Gridwise/gtm-base, scoped to Gridwise Analytics, linked to ~/Gridwise. The return session passed: opening ~/Gridwise woke the base.
- Amendments r2.4 and r2.5 are decided and reviewed, and NOT built.

## Next build, in Codex's order

1. Contract first: amend J8 to J10, J17, join SC1 and SC3, Units 4 and 5, the skill, the closing rules.
2. Completion and closing: two required drafts, the honest baseline finding, the map never asked about, no template placeholder date.
3. Quiet by default (r2.5): silent session start, the moment-of-use flag, "review my base", the opt-in weekly line, silence.
4. The rename: "context change" everywhere a person reads it; work/decisions to work/changes with a migration for the Gridwise base.
5. The habit hooks: a local edit asks what changed and why; the optional closing question that never confirms itself.
6. Selection fixes: plans and metrics only beside a profile, explicit folder choice honored once, what each folder mostly holds, final beats newer, templates last, the preview says why.
7. Umbrella profile, per-segment files, guarded adoption, scope recorded in the profile, unanswered "[your call" markers as a state.
8. The plain-language standard (r2.4 H) applied to every step, with the lint extended to check it.
9. The fidelity replay that gates the summarize-first step.

Execution posture: one Opus agent per unit at high effort, the orchestrator runs the suite after each, security and correctness review passes before shipping, Codex for design questions. While the plugin is live its gate reads every Bash command: keep git on its own command line, commit with a message file, never name the plugin's records folder or its variable on a command line, run Codex from a script file.

## Standing rules from Brandon

- The UX of the whole base is critical. Each step says what it is for first, shows output readable in seconds, ties it back, then asks one thing.
- The AI proposes, a person approves. Nothing is applied on its own.
- Do not interrupt work. The base is quiet unless the document in use is wrong.
- Never present an invented number or timing. Walkthrough records come from real runs.
