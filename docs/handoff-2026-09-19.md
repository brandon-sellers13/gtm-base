# Handoff, 2026-09-19 (evening)

Paste the block at the bottom into a new chat. Working directory: ~/Build/gtm-base (public repository brandon-sellers13/gtm-base, branch main, plugin 0.2.6 installed on this machine as gtm-base@gtm-base).

## State

- Built and shipped: Phase A1 of the first feature and release one of the join plan (0.2.6, 1127 tests, `sh tests/run.sh`).
- One real base exists: ~/GTM Bases/Gridwise/gtm-base, scoped to Gridwise Analytics, linked to ~/Gridwise. Opening ~/Gridwise wakes it (return-session test passed).
- Decided, reviewed by Codex, NOT built: join plan Amendment r2.4 (sections A to I) and first plan Amendment r2.5.
- The build plan for all of it: docs/plans/2026-09-19-001-feat-base-that-produces-work-plan.md, revision 1, twenty units in four phases.
- Two reviews of that plan were started on 2026-09-19 and write their results to files. Check whether each exists and is complete; rerun the missing one.
  - Astra through Codex: NOT done. The run on 2026-09-19 hit the Codex usage limit, which resets on 2026-09-25 at 10:41 AM unless Brandon adds credits. No review file exists. Rerun it then with the brief at docs/reviews/2026-09-19-plan-review-brief.md, writing to docs/reviews/2026-09-19-plan-review-astra.md (run Codex from a script file, never with its flags on a command line). Until then, revision 2 is built on the Fable review alone and says so, and Phase 1 units that the Astra review could change (the rename and migration, adoption, the moment-of-use flag) get a Fable security and correctness pass before release.
  - Fable 5.1: docs/reviews/2026-09-19-plan-review-fable.md.

## Next steps, in order

1. Read both reviews. Fold their findings into the plan as revision 2. Product questions go to Brandon with a recommendation and a runner-up; everything else is decided and recorded.
2. Expect both to say that reviewing proposals inside Claude must move into Phase 1, because a "no" in a review produces a proposal that today can only be approved on the GitHub page.
3. Build Phase 1 with /compound-engineering:ce-work, in the plan's order.

## Open calls for Brandon (none block revision 2)

1. What "Sol" refers to in "Opus 5 high agents along with Sol agents". Needed before the first unit is handed out.
2. The design file's shape: sections modeled on brandkit/brand-lock.json, living beside positioning.
3. The fidelity check needs a company with several segments and no finished pages. Supply one, or run two cases.
4. Live checks once per release or once per unit.
5. Whether to add ~/GTM Bases/ to the home-folder rule in his global CLAUDE.md (propose the line, he says yes).

## Who decides what

Claude runs without asking: plans, builds, tests, reviews, fixes, technical choices inside a decision already made, commits, pushes, version bumps, plugin updates, atlas republish, docs and memory. Brandon decides: what the product does for a person, words a user reads that carry meaning, scope and order, trade-offs where both sides are defensible, anything public or hard to undo. Only Brandon can do live runs in his own Claude Code and supply real numbers.

## Execution posture

Opus 5 agents at high effort, one unit per agent, together with "Sol" agents (to be confirmed). Reviews by Astra (Codex CLI) and Fable 5.1: the plan before any build, security and correctness before each release, design questions during the build. The orchestrator runs the suite and the plain-language lint after every unit and merges nothing with uncovered test scenarios. The logic atlas (docs/diagrams/logic-atlas.html, artifact 847c38c1-6d13-4fdd-aa89-027ad1674983) changes in the same change as the code. Bump the version for every release, because installed seats fetch by version.

While the plugin is live its gate reads every Bash command: keep git on its own command line, commit with a message file, never name the plugin's records folder or its variable on a command line, never write the literal ".git" on a command line, run Codex from a script file. Tell subagents that tasks/todo.md is append-only.

## Standing rules from Brandon

- The UX of the whole base is critical. Each step says what it is for in one sentence, shows output readable in seconds, ties it back, then asks one thing.
- The AI proposes, a person approves. Nothing is applied on its own.
- The base is quiet unless the document in use is wrong.
- The word is "context change", never "decision".
- Never an invented number or timing. Walkthrough records come from real runs.

## Paste this

Continue building GTM Base in ~/Build/gtm-base. Read docs/handoff-2026-09-19.md first and follow it. Then read docs/plans/2026-09-19-001-feat-base-that-produces-work-plan.md and the two plan reviews in docs/reviews/ (astra and fable, dated 2026-09-19); if either review is missing or incomplete, rerun it. Fold the findings into the plan as revision 2, bring me only the product questions with a recommendation and a runner-up, and then start Phase 1 with /compound-engineering:ce-work. Keep the memory file gtm-base-product-direction.md current.
