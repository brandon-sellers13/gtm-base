# Handoff, 2026-09-19 (night)

Paste the block at the bottom into a new chat. Working directory: ~/Build/gtm-base (public repository brandon-sellers13/gtm-base, branch main, plugin 0.2.6 installed on this machine as gtm-base@gtm-base).

## State

- Built and shipped: Phase A1 of the first feature and release one of the join plan (0.2.6, 1127 tests, `sh tests/run.sh`). The suite now fixes its time zone, because three tests assumed a Pacific date and failed on a machine set to Hawaii time.
- One real base exists: ~/GTM Bases/Gridwise/gtm-base, scoped to Gridwise Analytics, linked to ~/Gridwise. It has no shared copy. Opening ~/Gridwise wakes it.
- Both plan reviews are done. Astra through Codex (docs/reviews/2026-09-19-plan-review-astra.md) said not ready. Fable 5.1 (docs/reviews/2026-09-19-plan-review-fable.md) said ready only with changes. They agree on the three big things: no proposal can complete on a base with no shared copy, the fidelity gate could pass without proving anything, and twenty units is too many for one plan.
- The build plan is at revision 2.1: docs/plans/2026-09-19-001-feat-base-that-produces-work-plan.md. It covers Phase 1 only. Brandon's call: all of the context goes into the base first, and skills come after, so the runner and the outbound sequence are in the roadmap, which puts what a base holds ahead of any skill. Its section "Revision 2: what the two reviews changed" maps every finding to a unit. Phases 2 to 4 moved unchanged to docs/plans/2026-09-19-002-roadmap-after-phase-1.md.
- Built, reviewed where the plan calls for it, merged and pushed as of 2026-09-20 (1407 tests): Units 1.1, 1.1b, 1.2, 1.2b, and 1.3. Units 1.2b and 1.3 each had a security pass and a correctness pass, and each pass found real defects that were fixed test-first before the commit. Unit 1.3 also built a hook on the file-read tool as the backstop for the moment-of-use flag, after the hook documentation was checked twice. Nothing is released: the version is still 0.2.6 and the CHANGELOG entries sit under Unreleased. Next is Unit 1.4, then 1.5, then the release A security and correctness pass, the live check, and the version bump. The checklist is at the bottom of tasks/todo.md.
- Decided by Brandon on 2026-09-19 and 2026-09-20: no skill is pulled forward (context first, skills after); work in flight and a person's own notes stay outside the base as sources.
- Decided by Claude inside Unit 1.3 and open to Brandon's veto: a not now quiets the review and not the moment-of-use flag; somebody who does not own a document is offered two answers, not three.
- The fidelity replay's file lists are kept outside this public repository at ~/GTM Bases/Gridwise/fidelity-replay/. Still needed from Brandon: case two's material, where the answer key lives, and the lowered read amount for case three.
- For the release A security pass: the plugin's gate reads shell commands only, so a file-writing tool call can write the plugin's own records, which already lets the first-backup flag be flipped.

## The build order

Release A, shipped together: 1.1 contract amendments, 1.1b the UX standard and its lint, 1.2 completion and closing, 1.2b approve a proposed change locally, 1.3 quiet by default, 1.4 the rename with a recoverable migration, 1.5 habit hooks.

Release B, behind the fidelity gate: 1.6 selection fixes, 1.7a scope and the segment inventory, 1.7b umbrella and segment drafts and the marker state, 1.7c guarded adoption, 1.7d recovery of an interrupted import, 1.8 the sweep, 1.9 the fidelity replay scored by Brandon.

## Open calls for Brandon

The plan is written to the recommended answer on each, so the build does not wait. The full table, with a runner-up for each, is under "For Brandon" in the plan.

1. Approving a proposed change inside Claude moves into Phase 1 as Unit 1.2b.
2. Closed: no skill is pulled forward. Context first, skills after.
3. Brandon scores the fidelity replay; case two is the Gridwise material with the twelve finished pages held out.
4. Two releases rather than one.
5. The six UX rulings, including that adoption shows what was removed, the opening lines, and where the whole cleaned file is, rather than every body whole in the conversation.
6. The bar for the moment-of-use trial: ten real prompts, the flag fires on at least nine.
7. What "Sol" refers to. Units are built by Opus 5 agents at high effort until this is answered.
8. One line for ~/GTM Bases/ in the home-folder rule of his global CLAUDE.md.

## Who decides what

Claude runs without asking: plans, builds, tests, reviews, fixes, technical choices inside a decision already made, commits, pushes, version bumps, plugin updates, atlas republish, docs and memory. Brandon decides: what the product does for a person, words a user reads that carry meaning, scope and order, trade-offs where both sides are defensible, anything public or hard to undo. Only Brandon can do live runs in his own Claude Code and supply real numbers.

## Execution posture

Opus 5 agents at high effort, one unit per agent, together with "Sol" agents (to be confirmed). Reviews by Astra (Codex CLI) and Fable 5.1: the plan before any build (done), security and correctness before each release and on the five riskiest units (1.2b, 1.3, 1.4, 1.7c, 1.7d), design questions during the build. The orchestrator runs the suite and the plain-language lint after every unit and merges nothing with uncovered test scenarios. The logic atlas (docs/diagrams/logic-atlas.html, artifact 847c38c1-6d13-4fdd-aa89-027ad1674983) changes in the same change as the code. Bump the version for every release, because installed seats fetch by version; a merged unit is not a shipped unit.

While the plugin is live its gate reads every Bash command: keep git on its own command line, commit with a message file, never name the plugin's records folder or its variable on a command line, never write the literal dot-git folder name on a command line, run Codex from a script file, and write any long script to a file with the file tool rather than passing it inline, because prose that names the version-control tool gets the whole command refused. Tell subagents that tasks/todo.md is append-only.

To run Astra again: a script file that changes into the repository and runs `codex exec -s read-only --color never -o <output file> -` with the brief on standard input. Run it in the background; the review file appears when it finishes.

## Standing rules from Brandon

- The UX of the whole base is critical. Each step says what it is for in one sentence, shows output readable in seconds, ties it back, then asks one thing.
- The AI proposes, a person approves. Nothing is applied on its own.
- The base is quiet unless the document in use is wrong.
- The word is "context change", never "decision".
- Never an invented number or timing. Walkthrough records come from real runs.

## Paste this

Continue building GTM Base in ~/Build/gtm-base. Read docs/handoff-2026-09-19.md first and follow it. The build plan is docs/plans/2026-09-19-001-feat-base-that-produces-work-plan.md at revision 2.1; read its "Revision 2" table and the checklist at the bottom of tasks/todo.md, then carry on with the next unchecked unit using /compound-engineering:ce-work, one Opus 5 agent per unit, running the suite after each. Keep the memory file gtm-base-product-direction.md current.
