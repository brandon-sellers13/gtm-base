You reviewed Release A of GTM Base at 5f8076b; your report is docs/reviews/2026-09-23-release-a-verify-3-astra.md (findings R1 to R11). Every one is reported fixed in commit bd3bbcf, which is HEAD and the release candidate. Read only; change nothing. Output a written review in markdown. Use line numbers at HEAD.

## Do
1. For each of R1 to R11, say CLOSED or STILL OPEN at HEAD, rerunning your own scenario, and say when a test asserts something weaker than the finding.
2. Review what bd3bbcf added, which nobody outside has seen: the `remove` edit type and duplicate-heading indexing for hand edits (R10); repeatable wording revision with a `revision` counter and words files read without consuming them until success (R7); one ordering for files, folders, and survey places, `--drop` by number, `--add-file`, `--content-folder-file` (R9); index entry and permission capture and restore in local approval (R1); the write guard's durable base identity and history-destination comparison, including the one git call it now makes for writes into a history or assistant folder (R2, R3); error paths returning the wrapper's failure status (R4); `.` and `..` handling and link resolution in the shell fallback (R5); the config command parser (R6); the literal fenced diff and whitespace display (R8); the shared inventory of waiting changes (R11); and the extended walk in tests/test_skills_and_scripts_agree.py, which now claims to run optional flags named in prose.
3. Look specifically for: a legitimate action now refused (an ordinary write to a repository's assistant settings, an ordinary config command, a hand edit, a second or third wording revision, setup selection by number); state lost or restored over newer work in the new index and permission restore; a revision that bypasses the fresh-preview requirement; a words file that can now be read twice or substituted because it is no longer consumed on read; a number that still selects something other than what was shown.
4. Update the residual-risk list and the CHANGELOG assessment for HEAD.

## Output
Findings ranked critical, high, medium, low, each with file and line at HEAD, a concrete scenario, and the smallest fix. Then the verdict: ready to release, ready with the listed changes, or not ready, naming exactly what blocks. Be adversarial and specific. Do not pad.
